# Block dep lifecycle scripts (preinstall, install, postinstall) from running.
ignore-scripts=true

# Refuse to install any release younger than 72h (3 days).
# Requires npm >= 11.5. Older npm ignores this key with a warning.
min-release-age=3
